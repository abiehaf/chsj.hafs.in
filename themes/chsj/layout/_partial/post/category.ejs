<% if (item.categories && item.categories.length){ %>
  <%
  var cats = [];
  item.categories.forEach(function(cat){
    cats.push('<a itemprop="keywords" href="' + config.root + cat.path + '" rel="tag">' + cat.name + '</a>');
  });
  %>
  on <%- cats.join(', ') %>
<% } %>
